/**
 * after creating our module we want to use it everywhere
 * @type {module}
 */
var fftf = angular.module('fitForTheFuture');

fftf.controller()
angular.module('fitForTheFuture').controller()

/*
    fftf.controller() === angular.module('fitForTheFuture').controller()
 */